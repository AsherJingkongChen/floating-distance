[package]
name = "benchmarks-utils"
version = "0.3.0"

[dependencies]
floating-distance = { path = "../../" }
rand = "0.8.5"

[features]
simd = ["floating-distance/simd"]

[lib]
path = "lib.rs"
doctest = false
test = false
